{
  "name": "emoji-styles-monorepo",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter emoji-styles build && pnpm -r typecheck",
    "dev": "pnpm --filter demo dev",
    "generate": "pnpm --filter emoji-styles-scripts generate"
  }
}
